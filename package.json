{
  "name": "zrender-online",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["apps/web"],
  "scripts": {
    "dev": "wrangler pages dev ./apps/web/dist --local --compatibility-date=2024-11-29",
    "dev:build": "npm run build:pages && npm run dev",
    "build:pages": "cd apps/web && npm ci && npm run build",
    "build": "npm run build:pages",
    "deploy": "wrangler pages deploy ./apps/web/dist --project-name=zrender-online",
    "deploy:production": "npm run build && npm run deploy",
    "install:all": "npm install && cd apps/web && npm install",
    "clean": "cd apps/web && rm -rf dist node_modules && npm install",
    "type-check": "cd apps/web && npm run type-check",
    "lint": "cd apps/web && npm run lint"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}